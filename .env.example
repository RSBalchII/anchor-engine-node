# Anchor Configuration
# =====================
# ⚠️ SECURITY WARNING ⚠️
# =====================
# - shell_execute tool can run ARBITRARY COMMANDS on your system
# - DO NOT expose ECE_Core or Anchor to public networks without authentication
# - Review tool calls before execution when using small models (<14B parameters)
# - This is a LOCAL DEVELOPMENT TOOL - not designed for production/multi-user
#
# Small Model Limitation:
# - Models <14B parameters are UNRELIABLE for MCP tool calls
# - Recommended: Use 14B+ parameter models for tool reliability
# - Enable TOOL_CONFIRMATION_REQUIRED=true for smaller models

# ECE_Core Connection
ECE_URL=http://localhost:8000
ECE_TIMEOUT=60
ECE_API_KEY=your-secret-api-key-here-change-this

# Session Settings
SESSION_ID=default
AUTO_SESSION_ID=true

# Display Settings
SHOW_TOKEN_COUNT=true
SHOW_TIMESTAMPS=true
THEME=dark

# UTCP Services (Universal Tool Calling Protocol)
UTCP_SERVICE_ENDPOINTS=http://localhost:8007,http://localhost:8008,http://localhost:8009

# Tool Safety Settings
# ⚠️ IMPORTANT: Set to 'true' for smaller models or untrusted environments
AUTO_TOOL_EXECUTION=true
TOOL_CONFIRMATION_REQUIRED=false

# Dangerous Tools (require confirmation if TOOL_CONFIRMATION_REQUIRED=true)
# DANGEROUS_TOOLS=shell_execute,filesystem_write_file

# Safe Tools (auto-execute without confirmation)
# SAFE_TOOLS=filesystem_read_file,web_search,filesystem_list_directory

# ============================================================
# SECURITY - Shell Command Whitelist
# ============================================================
SHELL_WHITELIST_ENABLED=true
SHELL_ALLOWED_COMMANDS=ls,dir,pwd,cd,cat,head,tail,grep,find,echo,python,node,npm,git,curl,wget
SHELL_EXECUTION_TIMEOUT=30
SHELL_MAX_OUTPUT_SIZE=10000

# ============================================================
# SECURITY - Filesystem Path Protection
# ============================================================
FILESYSTEM_ALLOWED_PATHS=C:\Users\rsbiiw\Projects,/home,/tmp
FILESYSTEM_DENY_PATHS=C:\Windows\System32,/etc,/root
FILESYSTEM_ALLOW_TRAVERSAL=false
